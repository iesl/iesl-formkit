- import lib.ScalateOps._

-@ val it : forms.GenericConstrainedNestedForm
-@ val prefix: String
-@ val formUniqueId : String = ""

- it.fixedValue match {
-    case Some(value) => {
-        context << value
-        viewWithArgs(it, "fixedfield", context)('prefix -> prefix, 'formUniqueId -> formUniqueId)
-        }
-    case None => viewWithArgs(it, "field", context)('prefix -> prefix, 'formUniqueId -> formUniqueId)
-    }
